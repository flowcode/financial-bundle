Flowcode\FinancialBundle\Model\Core\Account:
    type: mappedSuperclass

    fields:
        code:
            type: string
            length: 255
            nullable: true
        name:
            type: string
            length: 255
        type:
            type: integer
        subType:
            type: integer
        editable:
            type: boolean

    oneToMany:
        journalEntry:
            targetEnity: JournalEntryInterface
            joinColumn:
                name: transaction_id
                referencedColumnName: id
                inversedBy: journalEntries

    lifecycleCallbacks: {  }
